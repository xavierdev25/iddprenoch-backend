meta {
  name: Iglesias
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/iglesias
  body: none
  auth: none
}

tests {
  test("devuelve 200 sin necesidad de cookie", function () {
    expect(res.status).to.equal(200);
  });
}
