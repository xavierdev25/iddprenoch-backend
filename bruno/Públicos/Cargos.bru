meta {
  name: Cargos
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/cargos
  body: none
  auth: none
}

tests {
  test("devuelve 200 sin necesidad de cookie", function () {
    expect(res.status).to.equal(200);
  });
}
