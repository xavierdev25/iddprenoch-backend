meta {
  name: Actualizar
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/eventos/{{eventoId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "ubicacion": "Nueva Sede"
  }
}

tests {
  test("actualiza y devuelve 200", function () {
    expect(res.status).to.equal(200);
  });
}
