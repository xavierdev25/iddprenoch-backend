meta {
  name: Actualizar
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/comunicados/{{comunicadoId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "descripcion": "Descripción actualizada del comunicado."
  }
}

tests {
  test("actualiza y devuelve 200", function () {
    expect(res.status).to.equal(200);
  });
}
