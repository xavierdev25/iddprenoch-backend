meta {
  name: Estado Eclesial
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/estado-eclesial
  body: none
  auth: none
}

tests {
  test("devuelve 200 con sesión", function () {
    expect(res.status).to.equal(200);
  });

  test("es una lista simple sin paginación", function () {
    expect(Array.isArray(res.body)).to.equal(true);
  });
}
