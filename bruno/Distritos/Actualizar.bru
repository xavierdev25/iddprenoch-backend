meta {
  name: Actualizar
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/distritos/{{distritoId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nombre": "Distrito de Prueba Bruno (actualizado)"
  }
}

tests {
  test("actualiza y devuelve 200", function () {
    expect(res.status).to.equal(200);
  });
}
