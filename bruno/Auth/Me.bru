meta {
  name: Me
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: none
}

tests {
  test("con sesión activa devuelve 200", function () {
    expect(res.status).to.equal(200);
  });

  test("devuelve el usuario autenticado", function () {
    expect(res.body).to.have.property("nombre", "admin");
  });
}
