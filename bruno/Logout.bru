meta {
  name: Logout
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: none
}

tests {
  test("logout devuelve 200", function () {
    expect(res.status).to.equal(200);
  });

  test("confirma cierre de sesión", function () {
    expect(res.body).to.have.property("ok", true);
  });
}
