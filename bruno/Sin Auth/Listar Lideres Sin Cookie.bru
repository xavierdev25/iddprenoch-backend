meta {
  name: Listar Lideres Sin Cookie
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/lideres
  body: none
  auth: none
}

script:pre-request {
  const jar = bru.cookies.jar();
  jar.deleteCookie(bru.getEnvVar("baseUrl"), "auth_token");
}

tests {
  test("sin cookie de sesión devuelve 401", function () {
    expect(res.status).to.equal(401);
  });
}
