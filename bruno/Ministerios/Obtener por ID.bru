meta {
  name: Obtener por ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/ministerios/1
  body: none
  auth: none
}

tests {
  test("devuelve 200", function () {
    expect(res.status).to.equal(200);
  });
}
