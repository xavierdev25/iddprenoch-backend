meta {
  name: Eliminar
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/cargos/{{cargoId}}
  body: none
  auth: none
}

tests {
  test("elimina y devuelve 204", function () {
    expect(res.status).to.equal(204);
  });
}
