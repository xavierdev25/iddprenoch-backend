meta {
  name: Crear
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/cargos
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nombre": "Cargo de Prueba Bruno"
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("cargoId", res.body.id);
  }
}

tests {
  test("crea el recurso y devuelve 201", function () {
    expect(res.status).to.equal(201);
  });

  test("devuelve el recurso creado", function () {
    expect(res.body).to.have.property("nombre", "Cargo de Prueba Bruno");
  });
}
