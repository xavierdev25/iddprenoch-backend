meta {
  name: Obtener por ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/usuarios/1
  body: none
  auth: none
}

tests {
  test("devuelve 200 con sesión", function () {
    expect(res.status).to.equal(200);
  });
}
